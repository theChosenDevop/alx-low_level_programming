#include <stdio.h>

/**
 * main - entry point
 * @argc: argument count
 * @argv: argument vector
 * Return: program name
 */

int main(__attribute__((unused))int argc, char *argv[])
{
	int i = 0;

	printf("%s\n", argv[i]);
	return (0);
}
